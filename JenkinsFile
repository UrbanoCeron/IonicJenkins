pipeline {
   agent any
     
   stages {
      
      stage('Add platform android') {
      steps {
         sh 'ionic cordova platform add android'
      }

      stage('Compile project for android') {
      steps {
         sh 'ionic cordova build android'
      }
   }
   
 }
}
